import { FroaladirDirective } from './froaladir.directive';

describe('FroaladirDirective', () => {
  it('should create an instance', () => {
    const directive = new FroaladirDirective();
    expect(directive).toBeTruthy();
  });
});
